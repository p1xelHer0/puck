val prompt_character : string
(** [prompt_character] is the character that prepends the user input,
    like `#` or `$` in `bash` *)

val v : string
(** [v] is the prompt characted with added terminal colors *)
